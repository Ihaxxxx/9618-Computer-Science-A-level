FUNCTION Factorial (number)

    IF num < 0 THEN
        RETURN -1
    ENDIF


    IF number == 0 THEN
        RETURN 1
    ENDIF

    factorial <- 1

    FOR index = number TO 1 STEP -1
        factorial <- factorial * index
    NEXT index
    ENDFOR

    RETURN factorial

END




FUNCTION Recursice ()
BEGIN
    
END



// Hour <- 00
OPEN FN FOR READ
READFILE FN , Info
Hour <- LEFT(Info,2)
Count <- 0
WHILE NOT EOF(FN)

    IF Hour = LEFT(Info,2)
        count <- count + 1
    ELSE
        OUTPUT "Hour " + Hour + " Total " + count
        Hour <- LEFT(Info,2)
        count <- 1
    
    READFILE FN , Info
    
ENDWHILE